[project]
name = "crwv-daily-simulation"
version = "0.1.0"
description = "Daily CRWV 30-day bearish put simulation"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0",
    "numpy>=1.24",
    "scipy>=1.10",
    "sympy>=1.12",
]

[tool.uv]
managed = true
